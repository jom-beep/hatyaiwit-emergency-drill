{
  "name": "Hatyaiwit Emergency Drill",
  "install": "bash scripts/cloud-agent-setup.sh",
  "terminals": [
    {
      "name": "wrangler-dev",
      "command": "export WRANGLER_SEND_METRICS=false && npx wrangler dev --port 8787 --ip 127.0.0.1",
      "description": "Local Cloudflare Worker dev server (PWA + API + D1 + Durable Object + Queues) on http://127.0.0.1:8787"
    }
  ],
  "ports": [
    {
      "name": "wrangler-dev",
      "port": 8787
    }
  ]
}
